export const funcAdd = (x, y) => x + y;
export const funcSub = (x, y) => x - y;
export const funcMul = (x, y) => x * y;
export const funcDiv = (x, y) => x / y;

export const add = funcAdd;
export const sub = funcSub;
export const mul = funcMul;
export const div = funcDiv;